package com.viswa.fitfusion.ui.theme

import androidx.compose.ui.graphics.Color

// Colors inspired by the design in the shared image
val primary = Color(0xFFBFFF00) // Bright green for highlighting actions like "Beginner" tag.
val onPrimary = Color.Black      // Text color on primary button, typically black for good contrast.

val background = Color(0xFF121212) // Dark background, almost black.
val onBackground = Color(0xFFE0E0E0) // Light grey text to contrast against the dark background.

// Colors for various UI elements and their contrasts
val surface = Color(0xFF1C1C1C)      // Surface color, slightly lighter than the background.
val onSurface = Color(0xFFFFFFFF)    // White text on surface for clarity.

val secondary = Color(0xFF4A4A4A)    // Muted grey for less emphasized elements (like inactive items).
val onSecondary = Color(0xFFFFFFFF)  // White text on top of secondary.

val error = Color(0xFFCF6679)        // A reddish color for errors.
val onError = Color(0xFFFFFFFF)      // White text on the error background.

val success = Color(0xFF00E676)      // Greenish color for success indicators (like checkmarks or confirmation).
val onSuccess = Color(0xFF121212)    // Dark text on a success background.

val inverseSurface = Color(0xFFE0E0E0)  // Light background used in contrast to the primary dark.
val inverseOnSurface = Color(0xFF121212) // Dark text on the inverse surface.

// Specific elements
val buttonBackgroundColor = primary // Use the primary color as the button background.
val textButtonColor = Color(0xFFBFFF00) // The color for text buttons like "Back", matches the highlight color.

